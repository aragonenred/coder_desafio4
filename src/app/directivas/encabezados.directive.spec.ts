import { EncabezadosDirective } from './encabezados.directive';

describe('EncabezadosDirective', () => {
  it('should create an instance', () => {
    const directive = new EncabezadosDirective();
    expect(directive).toBeTruthy();
  });
});
